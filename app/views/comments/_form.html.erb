<%= form_for [post.comments.build], html: { class: 'form-horizontal', remote: true } do |f| %>
	<%= f.hidden_field  :post_id,  value: post.id%>
	<%=  f.text_area :content %>
	<%= f.submit  class: 'btn-success' %>
	<% end %>


 